{
  "name": "issue-track-app",
  "version": "1.0.0",
  "description": "The backend for an issue tracker application.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ./client && npm run build --prod && cp -r build ../"
  },
  "author": "Branden McCarthy",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "morgan": "^1.10.0",
    "multer": "^1.4.2",
    "mysql": "^2.18.1",
    "validatorjs": "^3.20.0"
  },
  "engines": {
    "node": "16.5.x",
    "npm": "7.19.x"
  }
}
